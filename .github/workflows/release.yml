name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Windows installer
        run: pnpm pack:win

      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: deploy/desktop/release/Mirscope-Setup-*.exe

  build-source:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create source archive
        run: git archive --format=zip -o Mirscope-${{ github.ref_name }}-source.zip HEAD

      - name: Upload source artifact
        uses: actions/upload-artifact@v4
        with:
          name: source-zip
          path: Mirscope-${{ github.ref_name }}-source.zip

  release:
    needs: [build-windows, build-source]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/windows-installer/*
            artifacts/source-zip/*
          generate_release_notes: true
          body: |
            ## Mirscope ${{ github.ref_name }}

            Local First AI Prompt 全景分析平台（全身镜）

            ### 下载

            | 文件 | 说明 |
            |------|------|
            | **Mirscope-Setup-*.exe** | Windows 安装包（推荐） |
            | **Mirscope-*-source.zip** | 源码包（开发者） |

            ### Windows 安装

            1. 下载 `Mirscope-Setup-*.exe`
            2. 双击运行安装向导
            3. 从开始菜单或桌面快捷方式启动 Mirscope

            ### 从源码运行

            ```bash
            pnpm install
            pnpm dev
            ```

            ### 链接

            - 介绍页：https://mrsongzhaoyang.github.io/Mirscope/
            - 文档：README.md

            ### License

            MIT
